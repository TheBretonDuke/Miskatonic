Place your background GIF here as 'bg.gif'.

Recommended:
- Keep file size reasonable (<5–10 MB) to avoid slow loads.
- Use ~1920x1080 or similar with good compression.
- If you commit to Git, consider .gitattributes with Git LFS for large binaries.
